
# AI Facial Aesthetic Model — Web Frontend (Vite + React)

## Requirements
- Node.js 18+
- Backend running at `http://localhost:4000` (or set `VITE_API_URL`)

## Install & Run
```bash
npm install                   
                 
npm run dev ye lo abb 
# open http://localhost:5173
```

To point to a different API URL:
```bash
# .env.local
VITE_API_URL=http://localhost:4000
```

## Build
```bash
npm run build
npm run preview
```
